importFrom(ggplot2, scale_size, ggtitle, qplot, geom_bar, aes, stat_sum)
importFrom(ggmap, qmap)
importFrom(plyr, ldply)
importFrom(reshape2, acast, melt)
importFrom(gridExtra, grid.arrange, arrangeGrob)

## sampling tools
export(getSampleOf, getSubSample, filterUser, getBetween, getClosestTo, getRandomInterval, getFromInterval, PartitionHistoryData)

## plotting tools
export(plotGisDistribution, separate.timeline.plots, timeline.plots, VisualizeExtraction, PlotPredictionDiff)

## feature extraction tools
export(FeatureExtraction, create.lm.features)

## predition tools
export(MixturePredict, PredictionsAndValidations, MixtureSelection)

## scoring tools
export(scoreFn)

## validation tools
export(PredictionError)

## exporting methods implicitly exports the generic
##exportClasses(mle, profile.mle, summary.mle)
## All methods for imported generics:
#exportMethods(coef, confint, logLik, plot, profile, summary, 
#              show, update, vcov)
## implicit generics which do not have any methods here
#export(AIC, BIC, nobs)
